plugins {
    id 'java'
    id 'io.spine.tools.spine-codestyle-checker' apply false
}

apply plugin: 'io.spine.tools.spine-codestyle-checker'

codestyleChecker {

    javadocLinkChecker {
        threshold = thresholdValue
        reportType = reportTypeValue
    }
}
